# Codex File Search

## Overview
`codex-file-search` is a utility for searching files, likely implementing fast or fuzzy search capabilities. It supports CLI usage.

## Responsibility
- **Searching:** Logic to find files.
- **CLI:** Standalone search command (`cli.rs`, `main.rs`).

## Key Files
- **`src/lib.rs`**: Search logic.
- **`src/main.rs`**: Binary entry point.
