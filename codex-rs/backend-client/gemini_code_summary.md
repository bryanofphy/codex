# Codex Backend Client

## Overview
`codex-backend-client` is a specialized client for communicating specifically with the Codex backend API. It likely defines the typed interface for backend endpoints.

## Responsibility
- **Backend Communication:** Sends requests to the Codex backend (`client.rs`).
- **Data Types:** Defines types used in backend communication (`types.rs`).

## Key Files
- **`src/client.rs`**: Implementation of the backend client.
